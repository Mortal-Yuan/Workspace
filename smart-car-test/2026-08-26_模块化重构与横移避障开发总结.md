# 2026-08-26 模块化重构与横移避障开发总结

## 1. 今日最终成果

今天完成了固件模块化重构、巡线与硬件按键可靠性改造、三轮全向横移设计与
实验、矩形绕板避障状态机，以及多轮场地死区修正。旧的单文件主控
`main/main.c` 已由模块化实现完整替换；此前的实验日志、方案和审查记录继续
保留，没有用本总结覆盖历史结论。

当前固件已经烧录到 ESP32-S3。上电后保持 `IDLE`，三路电机命令为零；短按
BOOT 启动巡线与自动避障，RESET/EN 硬复位停车，串口 `x` 软件停车。

> 2026-08-27 后续修订：巡线层的非零最低输出和启动/换向开环增力会覆盖电机
> 速度控制结果，造成过弯半径增大并降低稳定性，现已从当前源码移除。本文其余
> 8 月 26 日实验记录继续保留；三轮横移标定和避障状态机未因该修订改变。
> 修订后主机回归测试与 ESP-IDF 5.4.4 完整构建均通过。随后已通过 USB
> Serial/JTAG `COM4` 完成烧录和三段哈希校验；静止检查保持 `IDLE`、
> `cmd=0,0,0`。
>
> 2026-08-27 第二次修订：当前源码进一步把巡线公式和全部参数恢复为 8 月 25 日
> 提交 `10c8414` 的基线。横移运动学和矩形避障状态机没有改变；该次参数恢复
> 已通过主机回归和 ESP-IDF 5.4.4 完整构建，并已烧录到 USB Serial/JTAG
> `COM4`。应用为 244112 字节，SHA-256 为
> `C59A28678C09EF443744FB1CA46FA78ECAB0BFF48252AACD8E348E5A6C12212D`。
> 三段写入均通过哈希校验；静止检查保持 `IDLE`、`cmd=0,0,0`。
>
> 2026-08-27 第三次修订：针对恢复基线后运行偏慢、卡顿的问题，当前源码保持
> `10c8414` 的巡线公式和状态逻辑不变，将基础速度、命令上限、`Kp` 和最大修正
> 同步提高约 12%，尽量保持原转弯强度比例。主机回归和 ESP-IDF 5.4.4 构建
> 均已通过；镜像为 244112 字节，SHA-256 为
> `3EA7D43765188B3E825A259E15F44FB761CED30C022118A963767DBBFB9FF6EA`。该修订
> 已通过 USB Serial/JTAG `COM4` 完成三段烧录和哈希校验；静止检查保持
> `IDLE`、`cmd=0,0,0`。
>
> 2026-08-27 第六次修订：以 8 月 25 日下午最终提交 `da7a8aa`
> 为版本边界，重新启用其由 `10c8414` 引入的完整巡线逻辑和全部原始参数；
> 今天的矩形绕障状态机、横移运动学、超声安全策略和电机驱动均保持不变。
> 后来的“只做弯道降速”实现改为 `.disabled` 归档。当前巡线不含此前已确认会
> 干扰速度控制的非零最低命令与启动/换向增力。主机回归和 ESP-IDF 5.4.4
> 构建通过；镜像为 244112 字节，SHA-256 为
> `C59A28678C09EF443744FB1CA46FA78ECAB0BFF48252AACD8E348E5A6C12212D`，并已
> 通过 `COM4` 完整烧录和静止检查。详细结果见第 6 节及《项目开发与实验日志》
> 第 17 节。
>
> 2026-08-27 第七次修订：实车出现直线起步困难、弯道外侧轮动力
> 不足和内侧轮低占空比停转。当前版本保持 8 月 25 日加权误差、方向锁存及
> A/C 差速结构，把基础速度和修正能力同比提高约 12%，并增加选择性驱动轮
> 起步增力：仅原始目标绝对值达到 200 的轮子在进入驱动角色或换向时使用
> 500/150 ms；内侧 8/43/44 等小命令不抬高。今天的矩形避障代码和参数未改。
> 主机回归和 ESP-IDF 5.4.4 构建通过；镜像为 244464 字节，SHA-256 为
> `F6BF85567A9DD874668382F7C0362757BBF08A644FE9FD214183C77CA43A0358`，已烧录
> 到 USB Serial/JTAG `COM4` 并完成无命令启动检查。
>
> 2026-08-27 第八次修订（当前状态）：单独修正中心单探头图案的内侧轮输出，
> `0010` 由 `8,0,-320` 改为 `100,0,-320`，`0100` 由 `-320,0,8` 改为
> `-320,0,100`；外侧轮进入驱动角色时仍先执行 500/150 ms。该修正服从原有
> 方向锁存，其他单侧图案、差速公式和避障逻辑均不变。
> 主机回归和 ESP-IDF 5.4.4 构建通过；镜像为 244544 字节，SHA-256 为
> `D2B1883E4256C332C5D63BE6E95E5CFC9836889549BE17FDB06F0AA1AFA7F0C5`。原生
> USB 未连接后，通过 MAC 核验同一开发板的 CP210x `COM3` 完成烧录与静止检查。
>
> 2026-08-27 第五次修订：在简化巡线逻辑不变的前提下，活动轮速精确提高 30%：
> 直线 360→468、弯道 250→325，共享默认速度上限 450→585。`0000` 仍停车，
> 差速归档和避障参数未改。主机回归和 ESP-IDF 5.4.4 构建均已通过；镜像为
> 243744 字节，SHA-256 为
> `6A40D07C214BB4C0B6044403698A47BB2198F1853D48CF1F8E7C71D01C14DFFB`。该镜像
> 已通过 USB Serial/JTAG `COM4` 完成三段烧录和哈希校验；静止检查保持
> `IDLE`、`cmd=0,0,0`。
>
> 2026-08-27 第四次修订：差速巡线已封存到 `main/control/archive/` 且不参与
> 编译。当前活动巡线只保留弯道降速：`0110/1111` 时 A/C 同速 360，其余非零
> 模式同速 250，`0000` 安全停车；不再计算差速、锁定方向或原地搜索。该修订
> 已通过主机回归和 ESP-IDF 5.4.4 构建；镜像为 243744 字节，SHA-256 为
> `10D24DBE66313D4072860E6E00ECCB9661889FFBDDAD0B9A2D20F4AB0CA44C3A`。该镜像
> 已通过 USB Serial/JTAG `COM4` 完成三段烧录和哈希校验；静止检查保持
> `IDLE`、`cmd=0,0,0`。

## 2. 固件重构

### 2.1 重构目标和审查过程

旧实现把硬件访问、巡线、避障、按键、命令、日志和故障处理集中在一个不断
增长的源文件中，修改任一功能都容易影响其他路径。重构前由独立计划 agent
提出方案，再由独立审查 agent 检查；计划经过两轮修订，补齐行为合同、双核
诊断隔离、超声事务竞态和多故障源表达后，第三轮审查通过，主 agent 才实施。
完整审查过程保存在《固件模块化重构计划.md》。

### 2.2 当前模块边界

| 目录 | 职责 |
| --- | --- |
| `main/app` | 顶层模式、命令意图、唯一运动仲裁、配置 |
| `main/control` | 纯控制策略：巡线、避障监督器、kiwi 逆运动学 |
| `main/drivers` | 电机、编码器、红外、HC-SR04、BOOT 驱动 |
| `main/platform` | 引脚定义和 ESP-IDF 电机硬件适配 |
| `main/diagnostics` | 静态队列、低优先级格式化、UART/USB 分片输出 |
| `main/core` | 跨模块配置、传感器、运动、诊断类型 |
| `test/host` | 直接编译生产 C 模块的主机回归测试 |

顶层控制器是唯一可以选择最终电机命令的模块，`motor_driver_apply()` 是唯一
最终电机写入点。控制周期不动态分配内存；相同命令不重复写硬件；换向先把该
轮 PWM 清零；全零命令关闭 EN/STBY，非零命令在方向和 PWM 写好后才使能。

故障按电机、平台中断、红外、超声、按键和配置分别保存，不再由互相矛盾的
多个布尔标志覆盖。诊断格式化和串口发送移出高优先级控制周期，避免日志阻塞
运动控制。HC-SR04 使用非阻塞边沿捕获、session/token 和临界区结算，保证完整
Echo 脉冲不会与同一周期的超时处理竞争。

### 2.3 巡线和按键重构

巡线保留四路红外的物理顺序 `L/LC/RC/R`。当前活动实现已经恢复 8 月 25 日的
加权误差、比例差速、方向锁存、普通弯/单侧边缘同比例限幅和丢线定向搜索。
BOOT 改为明确的“只启动”事件：按下稳定 50 ms、释放稳定
80 ms 才重新装填，上电保护 500 ms；保护期内的有效短按排队，上电时已经按住
则必须先释放。顶层模式统一为 `IDLE/AUTONOMOUS/MANUAL/SELF_TEST/FAULT`，解决
多次按键后无法再次启动和运行标志混乱的问题。

当前巡线参数为：

| 场景 | 最终值 |
| --- | ---: |
| 默认速度上限 | 400/1000 |
| 直线基础速度 | 360/1000 |
| 普通弯基础速度 / 命令上限 | 250 / 400 |
| 单侧边缘基础速度 / 命令上限 | 190 / 320 |
| 全白 `0000` | 按锁存方向原地搜索 320；无锁存时默认向左 |
| 差速公式 | `error=(-6L-2LC+2RC+6R)/count`，`correction=clamp(error*100,±250)` |
| 方向确认 | 连续 3 周期；反向未确认时控制误差幅值至少 4 |
| 中心单探头内侧轮 | `0010: A=100`；`0100: C=100`，且服从方向锁存 |
| 非零转弯轮最低命令 | 无；内侧小命令不设最低值 |
| 选择性驱动轮增力 | 原始绝对目标≥200时，进入驱动角色/换向使用500、150ms |

由于编码器会受到电机干扰，本版仍没有把噪声计数用于堵转闭环。选择性增力按
原始目标区分驱动轮与低需求内侧轮，不会把每个非零轮统一提高；后来的 A/C
同速简化实验保存在 `main/control/archive/`，不参与当前编译。

## 3. 新避障算法

### 3.1 设计思路

黑线被木板截断后，车辆不再采用“固定转角后沿恒定距离绕行”的方案。该方案
既依赖木板边缘稳定测距，又无法保证回线角度，可能沿黑线反方向行驶。新方案
利用三轮全向底盘保持车头大致不变，以矩形平移绕过木板：先左移越过近侧板边，
再向前越过木板，最后向右横移重新找到原黑线。这样不需要用多次开环转向恢复
航向，回线后车头理论上仍朝原行驶方向。

状态机为：

```text
SENSOR_CHECK
  -> CLEAR（巡线）
  -> BRAKE
  -> STRAFE_LEFT_EDGE
  -> STRAFE_LEFT_CLEARANCE
  -> SETTLE_FORWARD
  -> FORWARD_BYPASS
  -> SETTLE_RIGHT
  -> STRAFE_RIGHT_LINE
  -> LINE_CONFIRM
  -> CLEAR（恢复巡线）
  -> FINISHED（避障完成后遇到1111，锁存停车）
```

### 3.2 触发、找边和回线

- 启动后先停车，连续取得 3 次可靠的前方清空观测才允许巡线。
- 巡线时首次出现 20～100 mm 的真实原始 Echo 就立即停车，不等待中值或跳变
  滤波接受该突变。
- 停稳150 ms后，以固定1231 ms向左横移；再停稳150 ms，以固定1191 ms前进，
  用于补偿障碍触发点由60 mm提前到100 mm所增加的约40 mm距离。
- 再停车150 ms，向右横移寻找黑线，960 ms只是无黑线时的最大允许时间。
- 任一红外首次检测到黑线时，同一控制周期立即输出零，避免惯性冲过黑线；
  随后原地连续确认5个控制周期，确认成功恢复巡线，丢线则进入`FAILSAFE`。
- 本次避障完成前，`1111`仍按普通巡线图案处理；避障完成后遇到`1111`表示终点，
  进入独立的`FINISHED`状态并锁存停车。

### 3.3 安全保护

- 右移960 ms内未找到黑线时进入`FAILSAFE`并保持停车。
- 运动期间连续 3 次超声不确定会故障停车，不把 Echo 卡高或无效脉冲误判为板边。
- 前进越板阶段再次检测到100 mm内障碍会立即进入`FAILSAFE`。
- 各运动方向切换之间插入 150 ms 停稳段，减少矢量突变和惯性。
- RESET/EN 和串口 `x` 始终可停止；全零命令同时关闭电机驱动 EN/STBY。

## 4. 三轮横向移动设计与实测数据

### 4.1 已确认的底盘映射

- A：右轮；B：后轮；C：左轮。
- 前进：`A=-S, B=0, C=-S`。
- 已确认顺时针旋转：`A=+S, B=-S, C=-S`。
- 三轮位于约 120° 的 kiwi 全向布局。

根据已确认的前进、旋转方向和 120° 几何关系，理论横移基向量为：

| 运动 | A | B | C |
| --- | ---: | ---: | ---: |
| 左移 | `-0.866S` | `-S` | `+0.866S` |
| 右移 | `+0.866S` | `+S` | `-0.866S` |

该映射被集中在 `control/kiwi_kinematics`，避障状态机只输出车体前进、左移和
旋转分量，不直接散写 A/B/C 数值，便于未来加入 IMU 航向修正或速度闭环。

### 4.2 横移实验过程

1. 首轮使用 S=340、300 ms 的 `q/e` 限时实验。左右物理方向正确，三个轮子
   都参与，但左移时向左偏航、右移时向右偏航，且右侧更严重。
2. 加入 10% 反向偏航补偿后，旋转略有减小但仍明显；把测试时间延长到 1 s
   后确认该现象稳定存在，不是短时惯性。
3. 独立测试 B 轮发现明显方向性死区：`B=-340` 运行 1 s 时 B 计数约 181，
   `B=+340` 只有约 5，现场也确认正向基本不转；提高到 `B=+420` 后约 160，
   才与负向响应接近。
4. 补偿提高到 20% 后，纯计算会把 A/C 降到约 226，现场又落入 A/C 死区。
   因此运动学模块增加纯横移 A/C 最低输出，并对 B 正向单独设置最低输出。
5. 场地完整避障仍容易受静摩擦卡住，最终再次提高稳态和启动功率。

需要特别注意：B 轮负向单轮测试时，完全未驱动的 A/C 编码器也分别出现约
101/103 tick，证明电机干扰会造成跨通道假计数。上述编码器数据只用于说明
“存在明显异常或死区”的辅助证据，不能作为精确速度和距离标定。

### 4.3 当前横移和前进输出

横移基准为S=380，150 ms启动段为S=500。左右补偿参数保持独立配置，当前均为50%。纯横移A/C最低300，B正向最低
460。当前实际稳态命令约为：

| 动作 | A | B | C |
| --- | ---: | ---: | ---: |
| 左移稳态 | -300 | -570 | +300 |
| 右移稳态 | +300 | +570 | -300 |
| 左移启动 150 ms | -300 | -750 | +300 |
| 右移启动 150 ms | +300 | +750 | -300 |

绕板前进稳态为 `A=-400, B=0, C=-400`，每个前进段开始先以
`A=-500, B=0, C=-500` 增力 150 ms。

## 5. 主要问题和解决方案

| 问题 | 原因或证据 | 当前解决方案 |
| --- | --- | --- |
| 木板边缘超声回波不稳定 | 斜边、近场盲区、Echo 丢失 | 最短横移时间、连续3次清空、质量检查和5s超时 |
| 回线后可能反向行驶 | 多次开环转向无法保证航向代数和 | 改为保持车头方向的左移—前进—右移矩形路径 |
| 左移逆时针、右移顺时针偏航 | 镜像横移存在同向随动旋转误差，30%补偿仍不足 | B正向最低460；左右独立补偿当前均为50% |
| 补偿后A/C停转 | 增大补偿会继续降低A/C计算值 | 纯横移A/C最低保持300 |
| 场地摩擦导致避障阶段停住 | 横移/前进稳态PWM和启动脉冲偏低 | 保留避障分段的横移/前进启动参数；巡线增力已于8月27日移除 |
| 编码器数据不可信 | 未驱动轮也产生大量tick，C曾出现不可能高频毛刺 | 暂不做编码器速度/距离闭环，以直尺和时间中位数标定 |
| 超声固件曾显示Echo高和8m脉冲 | 探头贴桌面导致声学通道被遮挡 | 恢复正常朝向后验证231～263mm连续有效；保留Echo-high保护 |
| 首次见线容易因惯性冲过 | 横移中直接等待稳定图案会太晚 | 任一红外首次见黑立即停车，再原地确认5周期 |
| BOOT多次按压后状态混乱 | 多运行标志和固定冷却窗口 | 单一顶层模式、50ms防抖、80ms释放重装、500ms启动保护 |
| 日志可能拖慢控制 | 控制任务内格式化/发送 | 静态队列交给低优先级诊断任务，控制任务只发布结构化快照 |

## 6. 验证和当前固件

- 主机测试直接编译生产 C 模块，覆盖运动学、巡线选择性增力的阈值/时序/精确
  输出、完整矩形避障流程、障碍距离 49/50/51 mm 边界、见线立即停车、BOOT
  防抖、电机安全换向/故障和超声事务边界。
- 最终主机回归结果：`host firmware tests: PASS`。
- ESP-IDF 5.4.4 完整构建通过。
- 最终应用镜像大小：244544 字节（`0x3bb40`），应用分区剩余 77%。
- SHA-256：`81C0E59C3011BF6C699554C72D404B44CA5AF6800A1E594ED277B2B54B85EAD9`。
- `obstacle_supervisor.c/.h` 状态机实现未改；只把避障配置块的 `stop_mm` 由 200
  改为 50，其他避障参数逐项保持不变。
- 原生 USB `COM4` 未连接时，先通过芯片型号与 MAC 确认 CP210x `COM3` 是同一块
  ESP32-S3，再使用 ROM `no-stub` 完整烧录；三段均通过写入哈希校验。未发送
  运动命令的启动检查持续为 `IDLE`、`cmd=0,0,0`，超声约 269～450 mm 且质量
  正常；本次编码器在零命令下保持 `0,0,0`。

## 7. 下一轮场地测试重点

1. 测量“板边后左移 800 ms”的实际距离，至少重复 5 次，取中位数修正为 10 cm。
2. 测量前进 2500 ms 的实际距离，至少重复 5 次，取中位数修正为 40 cm。
3. 记录是否进入`RIGHT_12CM/LINE_CONFIRM/CLEAR/FINISHED`，验证右移首次见黑的
   同周期制动、5周期确认、恢复巡线以及避障后`1111`终点停车。
4. 检查是否仍有明显冲线；若有，优先缩短确认时间或调整右移稳态，但不降低各轮到
   已确认死区以下。
5. 若开环矩形路径仍有明显累计偏航，下一步接入 MPU6500 航向闭环；若要采用
   编码器距离闭环，必须先解决线束、电源和软件毛刺过滤问题。

## 8. 2026-08-27 障碍阈值调整

当前障碍触发阈值已由 200 mm 改为 50 mm，实际近距触发区间为 20～50 mm（包含
50 mm）。`obstacle_supervisor` 的状态转换、异常观测处理、确认次数、运动速度、
制动/绕行时间和超时均未改变。主机测试固定验证 51 mm 不触发、50 mm 与 49 mm
触发；ESP-IDF 构建和 CP210x `COM3` 烧录均成功。应用镜像为 244544 字节
（`0x3bb40`），SHA-256 为
`81C0E59C3011BF6C699554C72D404B44CA5AF6800A1E594ED277B2B54B85EAD9`。烧录后无命令
检查持续为 `IDLE` 和 `cmd=0,0,0`。

## 9. 2026-08-27 状态屏接入

初版曾按 ST7789 240x240 假设实现，实物点屏失败后由模块丝印确认型号为
`LQ_TFT18SPIV33`。当前已替换为龙邱 ILI9163B 初始化和 162x132 横屏布局，接线为
`D/C=GPIO38`、`SDI=GPIO20`、`SCK=GPIO45`、`CS=GPIO3`、`RST=GPIO47`。低优先级
显示任务约每 500 ms 刷新模式、
红外、超声、避障状态和三轮命令，不进入 20 ms 控制关键路径。固件在 IDLE 不碰
屏幕引脚；`v`、BOOT 或 `f` 才停止 USB 访问并启屏。由于 GPIO20 兼作原生 USB
D+，启屏后 COM 消失是预期行为，按 RESET 恢复。

修正版主机回归和 ESP-IDF 5.4.4 构建均通过，最终应用镜像为 276832 字节，SHA-256 为
`BC0B5EF4F3E27A239DDD24E81F2283B553210E9BFCB9DF2622C9DF8D8B56284F`。临时拔下
屏幕 SDI 后重新识别到 USB Serial/JTAG `COM4`，已用 ROM `no-stub` 完整烧录，
三段镜像均通过 Flash 哈希校验。约 8 秒无命令检查持续为 `IDLE`、三轮命令和
编码器 `0,0,0`，GPIO47在IDLE为下拉低输出，超声保持有效且无超时/异常。烧录后
可重新接回 SDI，最终颜色、
方向和可视边界仍需通过首次亮屏确认。
