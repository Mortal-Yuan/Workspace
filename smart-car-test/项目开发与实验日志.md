# 智能小车项目开发与实验日志

更新时间：2026-08-25

工程：`smart-car-test`

主控：ESP32-S3-DevKitC-1 / ESP32-S3-WROOM-2

开发环境：VS Code ESP-IDF 插件、ESP-IDF 5.4.4

本文记录已经实车确认的接线和功能、巡线算法升级过程、遇到的问题及实验结论。当前代码是可编译、可烧录的实验版本，但锐角弯道鲁棒性仍未达到要求。

## 1. 当前接线

### 1.1 电机驱动板

公共控制：

| 驱动板接口 | ESP32-S3 | 说明 |
| --- | --- | --- |
| 3V3 | 3V3 | 驱动逻辑电源 |
| GND | GND | 与主控共地 |
| STBY | GPIO7 | 高电平启用驱动 |

三个电机通道：

| 车轮位置 | 驱动板接口 | ESP32-S3 |
| --- | --- | --- |
| A / 右轮 | PWMA | GPIO9 |
| A / 右轮 | AIN1 | GPIO10 |
| A / 右轮 | AIN2 | GPIO11 |
| A / 右轮编码器 | E1A | GPIO18 |
| A / 右轮编码器 | E1B | GPIO8 |
| B / 后轮 | PWMB | GPIO12 |
| B / 后轮 | BIN1 | GPIO14 |
| B / 后轮 | BIN2 | GPIO15 |
| B / 后轮编码器 | E2A | GPIO39 |
| B / 后轮编码器 | E2B | GPIO40 |
| C / 左轮 | PWMC | GPIO16 |
| C / 左轮 | CN1 | GPIO17 |
| C / 左轮 | CN2 | GPIO21 |
| C / 左轮编码器 | E3A | GPIO41 |
| C / 左轮编码器 | E3B | GPIO42 |

已经实车确认的直行组合：

| 动作 | A 右轮 | B 后轮 | C 左轮 |
| --- | ---: | ---: | ---: |
| 前进 | `-speed` | `0` | `-speed` |
| 后退 | `speed` | `0` | `speed` |
| 停止 | `0` | `0` | `0` |

### 1.2 四路红外巡线模块

模块：`LQ-IR4CHV6`，供电使用 3.3 V，输出低电平表示检测到黑色。

| 物理位置 | 模块接口 | ESP32-S3 |
| --- | --- | --- |
| 最左 | OUT4 | GPIO5 |
| 左中 | OUT3 | GPIO4 |
| 右中 | OUT2 | GPIO2 |
| 最右 | OUT1 | GPIO1 |
| 电源 | VCC | 3V3 |
| 地 | GND | GND |

程序和日志统一按物理顺序 `L,LC,RC,R` 显示，并将低电平归一化：`1` 表示黑色，`0` 表示白色。

### 1.3 HC-SR04 超声波模块

| HC-SR04 | ESP32-S3 | 当前用途 |
| --- | --- | --- |
| Trig | GPIO6 | 发送 10 us 触发脉冲 |
| Echo | GPIO13 | GPIO 双边沿中断测量回波时间 |
| GND | GND | 共地 |

超声波供电和 Echo 电平接口以实车当前接法为准。信号分配和测距程序已经工作，但供电电压、Echo 对 ESP32-S3 的电平兼容性及测距精度尚未进行正式仪器验证。

### 1.4 引脚占用注意事项

- GPIO0 是板载 BOOT 按键，短按可启动巡线；复位时持续按住会进入下载模式。
- GPIO41、GPIO42 已用于左轮编码器，不能再用于早期规划中的 MPU6500 I2C。
- GPIO14 已用于后轮 `BIN1`，不能再用于早期规划中的 DHT11。

## 2. 当前代码和功能

主程序：[main/main.c](main/main.c)。

当前已经实现：

- A、B、C 三路 PWM 调速、方向控制和停止。
- 三组 AB 编码器计数。
- 四路红外传感器读取、物理顺序归一化和实时日志。
- 基于四路加权误差的比例巡线控制。
- 全白丢线后的方向搜索。
- 非阻塞 HC-SR04 测距。
- 巡线期间的超声波避障。
- 板载 BOOT 按键和 USB Serial/JTAG 命令控制。
- 启动后电机默认停止，短按 BOOT 或发送 `f` 才开始巡线，发送 `x` 停止。

当前巡线参数：

| 项目 | 数值 |
| --- | ---: |
| 控制周期 | 20 ms |
| 直线速度 | 320/1000 |
| 普通弯道速度 | 220/1000 |
| 单侧边缘模式速度 | 170/1000 |
| 普通弯道命令上限 | 360/1000 |
| 单侧边缘命令上限 | 280/1000 |
| 比例系数 | 90 |
| 最大修正量 | 220 |
| 全白搜索速度 | 280/1000 |

## 3. 巡线算法升级记录

### 3.1 基础比例巡线

四个传感器从左到右使用权重 `-6、-2、+2、+6`。检测到黑色的探头参与加权平均，得到负数表示线在左侧，正数表示线在右侧。A/C 两轮围绕已确认的前进组合做差速修正，B 后轮保持停止。

这个版本结构简单，普通直线和缓弯表现稳定。全白时根据最后一次非零误差选择原地搜索方向。

### 3.2 速度和丢线恢复实验

针对直线过快、弯道过快和冲出赛道后只会原地旋转的问题，曾尝试：

- 区分直线速度和弯道速度。
- 加入 PD 控制。
- 全白后执行倒车弧线、正向弧线、反向扩展搜索等多阶段恢复。

这组方案改善了部分丢线场景，但状态多、现场变量多，难以判断异常来自控制算法还是红外光照与机械摩擦。

### 3.3 专用锐角状态机实验

曾为单侧外探头持续触发增加专用锐角状态：连续确认后先短距离前进，再原地旋转，直到中心探头重新检测到黑线。

实车结果：遇到弯道时容易迅速冲出，然后进入原地旋转；降低速度后仍没有明显改善。因此已经撤回这套前冲、固定旋转和多阶段恢复逻辑，回到基础比例巡线作为对照基线。

### 3.4 当前方向锁存实验

观察到锐角处可能出现以下序列：真实路线应向左，但左侧探头先离开黑线，右侧探头短暂保留黑色，基础算法会把最后方向改写为右；随后 `0000` 丢线便向错误方向搜索。同一弯角从相反方向驶入时，传感器离开黑线的先后顺序不同，因此可能出现一侧正常、另一侧异常。

当前实验方案：

- 使用独立的 `locked_direction` 保存已确认方向。
- 相反方向必须连续出现 3 个控制周期，也就是约 60 ms，才允许改变锁存方向。
- 未确认的相反读数只记录为候选，电机继续采用锁存方向。
- 仅一侧有黑色信号时降低速度并限制最大电机命令。
- `0000` 时只按锁存方向搜索。
- 避障结束后不再把搜索方向写死为右侧。
- 日志增加 `error`、`control`、`lock`、`candidate` 和 `lost`，区分原始观测和实际控制方向。

实车结果：效果依然不佳，尚不能稳定通过锐角，也未解决同一弯角双向驶入表现不一致的问题。因此这次修改应视为保留代码和数据观测能力的失败实验，不能标记为已经解决锐角问题。

## 4. 已遇到的问题和处理结果

| 问题 | 分析 | 已采取措施 | 当前结论 |
| --- | --- | --- | --- |
| 轮子最初不转 | 曾出现电机外部电源未开启 | 确认电源、STBY 和单轮测试 | 三路电机均可工作 |
| 三轮位置不明确 | 早期文档与实车位置不一致 | 逐个运行 A/B/C 实车辨认 | A 右、B 后、C 左 |
| 前进时左转 | 左轮方向与 A 轮组合相反 | 修正 C 轮方向 | `A=-speed,B=0,C=-speed` 已确认前进 |
| 红外左右顺序混乱 | OUT 编号与物理排列相反，且低电平有效 | 逐路覆盖黑白纸确认，程序归一化 | 日志统一为 `L,LC,RC,R`，1 表示黑 |
| 红外全黑/全白无法区分 | 模块阈值和现场光线影响输出 | 调整模块后重复确认 | 基础识别可以工作，仍受环境影响 |
| 直线和转弯速度不合适 | 同一速度无法兼顾直线与急弯 | 分开设置直线、普通弯和边缘速度 | 普通路线表现改善 |
| 全白后无法回线 | 单纯原地旋转不能改变传感器位置 | 尝试多阶段位移恢复 | 部分场景改善，但复杂度和不确定性过高，已回退 |
| 锐角转向不足 | 连续比例控制面对大角度航向误差能力有限 | 尝试专用锐角状态机 | 前冲后旋转表现不稳定，已回退 |
| 锐角可能反向搜索 | 传感器离线顺序可产生短暂反向误差 | 尝试 3 周期方向锁存和边缘降速 | 最新实车结果仍不理想，继续处理中 |

## 5. 当前待解决问题

当前优先问题仍是锐角弯道的方向判定和重新捕获：

1. 需要采集同一个锐角从两个方向驶入时的完整 20 ms 原始模式序列，而不只观察 10 Hz 日志抽样。
2. 需要区分“线在车体哪一侧”和“新路线实际转向”这两个概念，单纯依赖最近横向误差可能无法推断锐角拓扑。
3. 需要验证传感器阵列相对车体前沿的位置、离地高度、探头间距和黑线宽度，这些机械条件可能决定锐角处的可观测性。
4. 下一次算法修改前，应先保存左右两个方向的失败日志，建立可重复输入，再比较状态机、模式序列识别或加入编码器短时里程约束等方案。

当前版本已经编译并烧录成功，但本日志明确记录：方向锁存方案的实车效果不佳，锐角问题未解决。
