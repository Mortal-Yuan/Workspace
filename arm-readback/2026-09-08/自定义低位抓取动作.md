# 自定义低位抓取动作记录

记录日期：2026-09-09

## 最新：抓取后维持位置（2026-09-10 用户选定）

用户已确认实际抓到物块，并经过单关节调整，选定当前位置作为抓取后的维持位置。此记录覆盖下文9月9日“尚未执行、保持卸力”的历史状态；目前机械臂保持夹持，主程序停在REPL。

| 舵机 ID | 000 | 001 肩 | 002 肘 | 003 腕俯仰 | 004 腕旋转 | 005 夹爪 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 保存时实测位置 | 1486 | 1082 | 2026 | 1346 | 1496 | 1506 |
| 形成此姿态的最近控制目标 | 1496 | 1123 | 2019 | 1355 | 1495 | 1700 |

实测姿态保存为`POST_GRAB_HOLD_POSE`，控制目标单独保存为`POST_GRAB_HOLD_TARGETS`（000～004）。肩轴带载时存在明显跟随偏差，不能把实测1082直接替代原控制目标1123并认定会复现相同高度。

`post_grab_hold_command()`生成以下候选维持指令，默认4000ms：

```text
{#000P1496T4000!#001P1123T4000!#002P2019T4000!#003P1355T4000!#004P1495T4000!}
```

该指令不包含005：抓取后保留原有P1700闭合目标。1506是夹住物块后的实际位置，不代表应把闭合目标改为1506。

这是用户选定的终点记录，取代此前模型上移3cm的候选。低位抓取→维持位置的自动过渡轨迹尚未验证或接入；不能把保存终点视为整段轨迹已验证。本次只读取并保存数据，没有执行新动作、复位或卸力，也未上传本地模块或修改原厂动作组。

后续同日，用户要求执行一次抓取，已用独立一次性脚本运行“低位张开3000ms→闭合1000ms→维持4000ms”。最终读数为1486、1089、2027、1338、1495、1507，与保存姿态前五轴相差不超过8 P单位。完整序列已有一次命令与读回验证，实物抓取及轨迹效果仍需现场确认；未接入小车自动任务或原厂常驻程序。日志：`pickup-and-hold-once-2026-09-10.txt`。

用户随后明确反馈“成功了”：上述完整序列已获一次现场成功确认。保留该抓取点和维持目标作为后续接入依据；本地模块尚未上传，常驻自动任务尚未接入。

## 手动示教位置

机械臂卸力后手动摆到目标位置，逐个读取到的舵机脉宽如下：

| 舵机 ID | 000 | 001 | 002 | 003 | 004 | 005（夹爪） |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 示教读数 | 1496 | 788 | 1816 | 1265 | 1496 | 1235 |

原样回到手动示教位置的命令（3 秒慢速到位）：

```text
{#000P1496T3000!#001P0788T3000!#002P1816T3000!#003P1265T3000!#004P1496T3000!#005P1235T3000!}
```

## 自定义抓取命令

为了让每次抓取的夹爪初始状态一致，将夹爪从手动读数 `1235` 改为原厂张开值 `1200`，其余关节保持示教位置：

```text
{#000P1496T3000!#001P0788T3000!#002P1816T3000!#003P1265T3000!#004P1496T3000!#005P1200T3000!}
```

到达并稳定后，闭合夹爪：

```text
#005P1700T1000!
```

如需再次张开夹爪：

```text
#005P1200T1000!
```

对应的纯命令生成模块是 `factory/z_custom_grab.py`。导入该模块不会驱动舵机；可使用其中的 `CAPTURED_POSE_COMMAND`、`PICKUP_OPEN_COMMAND` 和 `PICKUP_CLOSE_COMMAND`。

## 2026-09-09 使用限制与下一步（历史记录）

- 本次只记录读数并生成命令，尚未让机械臂执行该位置。
- 当前舵机仍应保持卸力状态；不要在手仍位于机械臂工作范围内时使能舵机。
- 该位置比原厂抓取点低约 15～17 cm，肩关节负载较大，曾在邻近位置出现持续抖动。
- 不能直接把“竖直状态 → 低位抓取点”当成已经验证安全的轨迹；正式接入自动抓取前，应清空机械臂周围空间，并分段、慢速验证过渡点和保持稳定性。
- 原厂 `factory/action.txt` 未修改。验证通过后，再把此位置编入新的动作组（建议从未占用的 G0039 开始）。
